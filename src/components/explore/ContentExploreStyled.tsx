import styled from "styled-components";

const ContentExplorestyled = styled.div`
  padding: 0px 12px;

  ul {
    list-style-type: none;
    margin-top: 8px;

    li {
      padding: 8px 0px;

      #wh-label {
        font-size: 10px;
        color: rgb(130, 130, 130);
      }

      #wh-title {
        font-size: 12px;
        font-weight: bold;
      }
    }
  }
`;

export default ContentExplorestyled;
